<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{{ shop.name }}</title>
	</head>
	<body>
		<style>
			body {
				margin: 0;
				padding: 0;
				font-family: "roboto", sans-serif;
				color: {{theme.fontColor}}
				;
				background-color: {{theme.backgroundColor}}
				;
			}
			.container {
				width: 100%;
				max-width: 1200px;
				margin: 0 auto;
			}
			h1 {
				background-color: {{theme.colorMenu}}
				;
				color: {{theme.fontColor}}
				;
				font-size: 2rem;
				margin-top: 0;
				padding: 1rem;
			}
			.banner {
				width: 100%;
				height: 300px;
				background-size: cover;
				background-position: center;
				background-repeat: no-repeat;
			}
			p {
				font-size: 1.2rem;
			}
			img {
				width: 10%;
				height: auto;
			}
		</style>
		<div class="container">
			<div class="banner">
				{% if logo == null %}
					{% for shop in shops %}
						<img class="logo rounded" src="https://fakeimg.pl/400x400/?text={{ shop.name }}" alt="{{ shop.name }}">
					{% endfor %}
				{% else %}
					<img src="{{ absolute_url(asset('/images/logos/' ~ logo.image)) }}" alt="{{ logo.name }}">
				{% endif %}
				<h1>Bienvenue sur
					{{ shop.name }}!</h1>
			</div>
			<p>
				Merci de confirmer votre email en cliquant sur ce lien:
				<br><br>
				<a href="{{ signedUrl }}">Confirmer mon Email</a>.
			Ce lien expirera dans
				{{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.
			</p>
			<p>
				A bientôt
			</p>
		</div>
	</body>
</html>

{% extends 'base.html.twig' %}

{% block title %}Envoi de mot de passe |
	{{ parent() }}
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row g-0">
			<div class="card bg-light">
				<div class="col mt-5 mx-auto">
					<p class="fw-semibold text-center">
						Si un compte email existe, un email sera envoyé avec un lien pour réinitialiser votre mot de passe.
											Ce lien expirera dans
						{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
					</p>
					<p class="fw-semibold text-center mb-5">Si vous ne recevez pas d'email, vérifier vos spams où
						<a href="{{ path('forgot_password_request') }}">recommencer</a>.</p>
				</div>
			</div>
		</div>
	</div>
{% endblock %}

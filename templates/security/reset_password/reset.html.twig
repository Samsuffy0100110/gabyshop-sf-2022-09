{% extends 'base.html.twig' %}

{% block title %}Redéfinir un mot de passe
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="text-center mt-3">Nouveau mot de passe</h1>

		{{ form_start(resetForm) }}
		{{ form_row(resetForm.plainPassword) }}
		<div class="d-flex justify-content-end">
			<button class="btn btn-success mt-2 mb-5">Enregistrer</button>
		</div>
		{{ form_end(resetForm) }}
	</div>
{% endblock %}

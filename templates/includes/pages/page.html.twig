{% extends 'base.html.twig' %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col">
            <h1 class="text-center">{{ page.title}}<h1>
            </div>
		</div>
	<div class="row">
		<div class="col">
			{{ page.content|raw }}
		</div>
	</div>
</div>{% endblock %}

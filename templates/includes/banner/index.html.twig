<div class="container">
	<div class="row">
		<div class="col-12 mb-3">
			<img class="img-fluid w-100 rounded banner-image  shadow" style="max-height: 400px" src="{{ ('/images/banners/' ~ banner.image) }}" alt="{{ banner.name }}">
		</div>
	</div>
</div>

{% extends 'base.html.twig' %}

{% block title %}{{ parentCategory.name }}{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="col-lg-12">
				<a href="{{ path('home')}}">Home</a>
				/Categorie/
				<a href="{{ path('category_index', { parentCategory: parentCategory.slug })}}">{{ parentCategory.name }}</a>
			</div>
		</div>
	</div>
{% endblock %}
